#include <iostream>
using namespace std;

int gcd(int a, int b) {
    return (b == 0 ? a : gcd(b, a % b));
}
int main() {
    long long t, a;
    while(t--) {
        cin >> a;
        
    }
    return 0;
}

int gcd(int a, int b) {
    
}